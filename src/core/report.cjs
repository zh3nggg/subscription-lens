'use strict';
const {translate,resolveLanguage}=require('../i18n.js');
const esc=v=>String(v??'').replace(/[&<>"']/g,c=>({'&':'&amp;','<':'&lt;','>':'&gt;','"':'&quot;',"'":'&#39;'}[c]));
function reportData(query,filters={}){return {version:1,generatedAt:new Date().toISOString(),from:query.range.from,to:query.range.to,timezone:query.timezone,tokens:query.summary.tokens,usd:query.summary.unpriced>0&&query.summary.unpriced===query.summary.events?null:query.summary.usd,events:query.summary.events,unpriced:query.summary.unpriced,coverage:query.summary.tokens>0?query.summary.pricedTokens/query.summary.tokens:null,priceDate:query.catalog.asOf,filtered:!!(filters.model||filters.project||filters.session||filters.query||filters.day||filters.unpriced)};}
function reportHtml(data,language){const locale=resolveLanguage(language,'en-US'),t=key=>esc(translate(key,locale));const number=n=>new Intl.NumberFormat(locale).format(n);const money=n=>n===null?'—':new Intl.NumberFormat(locale,{style:'currency',currency:'USD'}).format(n);const date=s=>new Date(s).toLocaleString(locale);return `<!doctype html><html lang="${locale}"><meta charset="utf-8"><meta name="viewport" content="width=device-width,initial-scale=1"><meta http-equiv="Content-Security-Policy" content="default-src 'none'; style-src 'unsafe-inline'; base-uri 'none'; form-action 'none'"><title>Subscription Lens</title><style>body{font:16px/1.6 system-ui,sans-serif;color:#222;background:#f7f7f7;margin:0;padding:40px}main{max-width:760px;margin:auto;background:white;border:1px solid #e6e6e6;border-radius:20px;padding:36px}h1{font-size:28px;margin:0 0 8px}small,p{color:#666}.grid{display:grid;grid-template-columns:1fr 1fr;gap:24px;margin:32px 0}.value{font-size:36px;font-weight:600}.row{display:flex;justify-content:space-between;gap:20px;padding:12px 0;border-top:1px solid #eee}@media(max-width:600px){body{padding:12px}main{padding:20px}.grid{grid-template-columns:1fr}.row{display:block}}</style><main><small>Subscription Lens · ${t('本机 · Codex')}</small><h1>${t('用量摘要')}</h1><p>${esc(date(data.from))} — ${esc(date(new Date(data.to).getTime()-1))}<br>${esc(data.timezone)}${data.filtered?' · '+t('已筛选'):''}</p><div class="grid"><div><small>Tokens</small><div class="value">${esc(number(data.tokens))}</div></div><div><small>${t('API 等价成本')}</small><div class="value">${esc(money(data.usd))}</div></div></div><div class="row"><span>${t('计价覆盖率')}</span><b>${data.coverage===null?'—':esc(new Intl.NumberFormat(locale,{style:'percent',maximumFractionDigits:1}).format(data.coverage))}</b></div><div class="row"><span>${t('用量记录')}</span><b>${esc(number(data.events))}</b></div><div class="row"><span>${t('未计价')}</span><b>${esc(number(data.unpriced))}</b></div><div class="row"><span>${t('价格')}</span><span>${esc(data.priceDate)} · Standard · USD</span></div><p>${t('API 等价成本按价格快照计算，不含工具费用。普通 ChatGPT 聊天和未接入设备不计入明细。')}</p><small>${t('仅含汇总，不含账户、项目名、路径或会话标识。')}</small></main></html>`;}
module.exports={reportData,reportHtml};
