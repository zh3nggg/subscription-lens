diff --git a/pnpm-workspace.yaml b/pnpm-workspace.yaml
index 435b40ef..791db22a 100644
--- a/pnpm-workspace.yaml
+++ b/pnpm-workspace.yaml
@@ -6,3 +6,7 @@ onlyBuiltDependencies:
 
 ignoredBuiltDependencies:
   - msw
+
+allowBuilds:
+  esbuild: true
+  msw: false
diff --git a/src-tauri/Cargo.toml b/src-tauri/Cargo.toml
index cfbfc90a..59373f19 100644
--- a/src-tauri/Cargo.toml
+++ b/src-tauri/Cargo.toml
@@ -18,6 +18,7 @@ doctest = false
 [features]
 default = []
 test-hooks = []
+sublens-host = []
 
 [build-dependencies]
 tauri-build = { version = "2.4.0", features = [] }
diff --git a/src-tauri/src/lib.rs b/src-tauri/src/lib.rs
index d3ce38a3..54e1dc9d 100644
--- a/src-tauri/src/lib.rs
+++ b/src-tauri/src/lib.rs
@@ -342,6 +342,11 @@ fn macos_tray_icon() -> Option<Image<'static>> {
 
 #[cfg_attr(mobile, tauri::mobile_entry_point)]
 pub fn run() {
+    run_with_context(tauri::generate_context!());
+}
+
+/// Run the CC Switch runtime with a host-owned Tauri context.
+pub fn run_with_context(context: tauri::Context<tauri::Wry>) {
     // 设置 panic hook，在应用崩溃时记录日志到 <app_config_dir>/crash.log（默认 ~/.cc-switch/crash.log）
     panic_hook::setup_panic_hook();
 
@@ -1718,7 +1723,7 @@ pub fn run() {
         ]);
 
     let app = builder
-        .build(tauri::generate_context!())
+        .build(context)
         .expect("error while running tauri application");
 
     app.run(|app_handle, event| {
