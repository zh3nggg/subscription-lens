[package]
name = "subscription-lens-tauri"
version = "3.0.2"
edition = "2021"
publish = false

[dependencies]
cc-switch = { package = "cc-switch", path = "../cc-switch-runtime/src-tauri", features = ["sublens-host"] }
serde_json = "1.0"
tauri = { version = "2.8.2", features = ["custom-protocol"] }

[build-dependencies]
tauri-build = { version = "2.4.0", features = [] }
